# Abbey identity (distilled)

<!-- BEGIN abbey-generated:claims-table -->
<!-- Generated by tools/check_claims_sync.py; do not edit. -->
Canonical capability ledger: **34 Current · 2 Partial · 8 Proposed · 1 Blocked · 5 Out of scope** ([full evidence](claims.md)). Source: `src/claims.rs`; schema 1; digest `5279e2d68b22592e030dca59f2c9d2a3cf4a3eb0c4b0812e00bc1b8181945e86`.
Executable workflow ledger: **26 goals (23 done, 1 in_progress, 1 proposed, 1 blocked) · 159 checked / 22 open todos** (stable goal metadata in `tasks/goals.md`).

| Stable ID | Status | Capability | Evidence boundary |
| --- | --- | --- | --- |
| `backend-cursor-agent` | Current | cursor-agent backend (CLI/TUI) | preferred default, never required — the unchosen default auto-falls back to installed grok\|fm\|abi, local verbs need no executor; ABBEY_BACKEND=fm\|grok\|abi selects explicitly |
| `surface-parity-grok-codex-claude` | Partial | Grok/Codex/Claude surface parity | selected UX aliases and compatible surfaces ship; parity polish continues; not a reimplementation of vendor runtimes |
| `personas-abbey-aviva-abi` | Current | Persona Abbey/Aviva/Abi | via abi-ai contracts |
| `role-bindings-max-gemma` | Current | Max/Gemma role bindings | model-alias bindings — not local Qwen/Gemma weights |
| `memory-sqlite-self-learn` | Current | SQLite memory + self-learn | STM/LTM/activity/train_candidate + learn pipeline |
| `memory-map-three-dimensional` | Current | 3-D memory map (topic × recency × consolidation) | deterministic axes — not a learned embedding space |
| `memory-wdbx-in-process` | Current | WDBX in-process memory | behind --features wdbx (off by default); fs4 advisory lock (flock / LockFileEx) |
| `hybrid-loop-route-audit-learn-review` | Current | Hybrid loop / route audit / learn review | Gemma→Max correlated; confidence/alt/fb audit-only; train curation |
| `subagents-local-path-peers` | Current | Multi-subagent + local PATH peers | abbey subagents — same-host peers, not multi-node mesh |
| `improve-goal-driven-loop` | Current | Goal-driven improve loop (local subagents + check.sh bar) | abbey improve — ledger pick + bounded --confirm apply; not multi-node / not unrestricted OS |
| `daemon-read-only-app-core` | Current | shared application core + authenticated read-only abbeyd client/server | abbey daemon status\|claims\|routes uses exact protocol-v1 read-only contracts over an owner-only bounded Unix socket; bounded startup-bound durable runs remain on the separate protocol-v2 surface, and a separate authenticated protocol-v3 envelope serves canonical safe read-only tool inventory, exact canonical stable-claim reads, conditional bounded ABI-local model inventory, and one separately claimed exact-approved safe memory-obsolete effect. Windows named pipes, tool or shell execution on protocol v1/v2, and memory reads/search remain unavailable |
| `app-core-route-audit-read` | Current | sanitized read-only persona/role route-audit contract | abbey daemon routes and the desktop Routes view read a bounded tail (at most 50 entries) of the append-only route.jsonl through one app-core command. The raw working directory never crosses the boundary: it becomes an opaque domain-separated ws-<12 hex> digest, free text is bounded, control-stripped, and path-redacted, and confidence is quantized to whole percent. The producer sanitizes and the consumer re-validates, so an absolute path, a control character, or a non-digest workspace is rejected on the wire rather than trusted. This is a tail read, not a cursor: no sequence column, no filtering by persona/role/workspace, no live subscription, and no write, delete, or rotation path. It is deliberately NOT registered as an MCP tool, because that registry is also served over an unauthenticated loopback HTTP transport |
| `daemon-protocol-v2-bounded-runs` | Current | authenticated protocol-v2 bounded local run control | typed DaemonClient + abbeyd expose only startup-bound ABI-local/Foundation Models submission, durable status/cancel, and snapshot-consistent bounded paged sanitized lifecycle events over the owner-only Unix socket; execution now uses Abbey's finite-budget ABI ModelProvider bridge over those fixed startup objects. The CLI and TUI slash surface emit the same typed AppCommand values and consume AppEvent values through one deterministic frontend-neutral lifecycle reducer. The protocol-v1 read-only command set remains compatible. Requests cannot select executables, argv, environment, or workspace; on protocol v2, tool/shell dispatch, raw prompts/provider output, live subscriptions, daemon-owned memory, automations/approvals, the desktop bridge, model registry lifecycle, and Windows named pipes/Job Objects remain unavailable |
| `daemon-protocol-v3-abi-model-inventory` | Current | authenticated protocol-v3 safe tool, memory, model, and stable-claim authority | abbeyd accepts a separate strict protocol-v3 envelope over the owner-only authenticated Unix socket and always negotiates list_tools and invoke_tools. The first three descriptors project the structurally read-only canonical SAFE_TOOLS registry shared with MCP; the default safe daemon alone appends abbey_memory_mark_obsolete, a local mutating descriptor accepting one bounded opaque record ID, and negotiates decide_tool_approvals plus cancel_tools. The personal catalog and MCP remain exactly three read-only tools and do not receive mutation, decision, cancellation, or execution authority. InvokeTool validates the registered schema before ABI EffectScopedPolicy. Read-only calls retain digest-only authorization and terminal outcome audit, ABI ToolExecutor execution, one-second cooperative deadline, 32 KiB terminal JSON bound, and durable single-use call IDs recovered after reopen. For the local mutation, policy must return RequireConfirmation; the first exact call computes a domain-separated SHA-256 digest over call ID, tool ID, and canonical JSON input, persists one schema-v5 pending record with at most fifteen minutes lifetime and one append-only event, returns a typed correlated pending status, stores no raw input, and performs no effect. ApproveTool and DenyTool bind a globally single-use decision ID to that exact call and digest; CancelTool binds a globally single-use cancellation ID and may terminate only pending or approved records. Denied, cancelled, expired, consumed, mismatched, missing, and reused calls fail closed. Only an identical explicit InvokeTool resubmission of a still-approved call can execute. The daemon atomically changes that approval to consumed and writes schema-v6 prepared intent before opening the startup-selected edition memory backend; requests cannot select or override the backend. It then marks the one record obsolete without deleting provenance, returns a bounded correlated succeeded or failed result, and persists only its SHA-256 digest plus append-only execution transitions. Missing or unavailable memory becomes a bounded terminal failure rather than an unconsumed retry. Debug-only process failpoints immediately after prepare and immediately after the effect prove both crash windows: reopen changes unfinished prepared intent to interrupted, never fabricates a result digest, rejects replay of the consumed call, and requires a fresh call and approval even when the idempotent effect may already have happened. V3DaemonSession sends every request once; approval and resubmission are separate explicit caller actions, and neither downgrade nor automatic mutation replay exists. The daemon also negotiates read_memory only when the startup-selected exact edition backend opens, exposes one explicit memory-v1-summary space, and serves bounded summary-only search plus sanitized metadata. Internal record IDs become domain-separated SHA-256 identifiers; paths are redacted, controls removed, labels capped at 256 bytes, and payload, provenance, source metadata, and raw IDs never cross the socket or participate in search. Search watermarks are cached, bounded, and bound to the exact query so changed stores, future cursors, and cross-query replay cannot drift a page. The same authority always negotiates read_claims_by_id for exact stable-ID reads and conditionally negotiates read_models only when an ABI-local fixed provider is bound at startup; that inventory comes from the same startup-owned ABI ModelProvider route used by protocol-v2 execution. Missing or forged grants, unknown spaces and records, future cursors, and non-exact claim IDs fail closed. Protocol v1 and v2 remain unchanged, and MCP receives no memory surface. There is no fuzzy claim search, semantic-vector search, general raw memory authority, served model inference/download/load command, personal shell, training, worker, polling, CLI memory presentation, desktop, Windows, or remote authority |
| `daemon-protocol-v3-memory-reads` | Current | sanitized protocol-v3 memory inventory, search, and metadata | the authenticated owner-only Unix daemon negotiates read_memory only when its startup-selected exact edition backend opens. It exposes one explicit memory-v1-summary space: search considers sanitized summaries only, while metadata returns the same bounded projection. Internal IDs become domain-separated SHA-256 identifiers; paths are replaced with [path], controls are removed, and labels stop at 256 bytes. Payload, provenance, source metadata, project paths, and raw record IDs are neither searched nor serialized. A bounded sixteen-entry cache binds each fixed watermark to the exact query and at most 1,024 live records, so later pages remain stable across writes and future, evicted, or cross-query cursors fail closed. Requests cannot select a backend or state root. Default, WDBX-feature, and personal binaries share the contract against their distinct edition roots. MCP, protocol v1/v2, memory mutation, semantic-vector search, CLI/desktop presentation, Windows, remote access, and live subscriptions are unchanged or absent |
| `runtime-legacy-conversation-metadata-migration` | Current | idempotent legacy conversation metadata migration | daemon startup snapshots only canonical edition-scoped chat-id/history/by-cwd metadata into a retained owner-only content-addressed backup before idempotently importing opaque conversation identity and trustworthy timestamps into runtime.sqlite; original metadata and finalized backups are never deleted. No transcript, semantic-memory, backend/title/run inference, override-path following, or protocol/UI authority is added |
| `runtime-conversation-identity-write-cutover` | Current | canonical conversation identity mutations and selected compatibility reads | on Unix, new conversation identity saves and scope/all clears validate and domain-separate the active edition and affected scopes before committing monotonic opaque save evidence or explicit tombstones to runtime.sqlite. The same owner-only fs4-locked recovery journal then projects exact edition-scoped compatibility-mirror creation or removal: reopen discards prepared-but-uncommitted plans, completes exactly marked commits idempotently, refuses divergent destinations, and prevents stale identity resurrection. Scope clear removes only the uniquely authorized active working-directory mirror, preserving the global fallback; non-per-cwd scope clear removes global plus export, while clear-all removes global/export plus an exact bounded inventory of direct by-cwd mirrors for the active edition. Reads recover the journal, select cwd before global from canonical edition-scoped scope evidence, continue past cwd tombstones, stop at global tombstones, and accept a selected mirror only when its external identity matches the canonical digest; a corrupt selected cwd mirror never falls back to global. Untracked scopes retain bounded legacy cwd-then-global compatibility. Provider create, resume, retry, and capture paths use the fallible resolver and fail closed on canonical or mirror corruption; the lossy wrapper is presentation-only. Aliases and conversation provenance, history, transcripts, semantic memory, model/route/run data, and retained migration backups are not deleted. Backend/title/run inference, protocol/UI/MCP surfaces, and Windows runtime authority are unchanged or unavailable |
| `inventory-media-delegation-surfaces` | Current | MCP/ACP inventory + voice + highlight + media/imagine | inventory/peer launch and delegated media surfaces; Abbey is not a client/host of other providers' MCP servers, and there are no local neural media models yet |
| `mcp-server-stdio-readonly` | Current | Abbey read-only stdio MCP server (tools capability) | abbey mcp serve — JSON-RPC 2.0 over newline-delimited stdio, protocol-version negotiation, tools/list + tools/call with JSON Schema 2020-12 descriptors, over a registry whose effect type admits read-only tools only (no shell, no filesystem mutation, no process spawn — abbey_platform therefore omits accelerator detection, which spawns vendor binaries); enforced request-byte / JSON-depth / batch / timeout / response limits plus outbound secret redaction. The same registry and the same dispatch are also served over loopback HTTP — see mcp-server-http-loopback-readonly. TLS, OAuth 2.1/PKCE, resource indicators, token-audience binding, and the stateless-lifecycle revision are NOT implemented on either transport; serving tools does not make Abbey a client/host of other providers' MCP servers |
| `mcp-server-http-loopback-readonly` | Current | Abbey read-only Streamable HTTP MCP transport (loopback only) | abbey mcp serve http — HTTP/1.1 POST /mcp, one request per connection, routed through the *same* Server::handle_frame and Server::encode_frame as stdio, so the tool registry, the byte/depth/timeout ceilings, and outbound secret redaction are shared code rather than a parallel implementation; a non-loopback --host is a hard startup error, not a warning; two-layer DNS-rebinding defense: Host must be loopback (parsed structurally — authority split plus IpAddr::parse, never substring matching) and Origin, when present, must be a loopback origin (parsed with the url crate), so lookalikes such as http://127.0.0.1.evil.com and http://localhost.evil.com are rejected by both layers; enforced named concurrency cap, fixed-window rate limit, read/write deadlines, request-head and header-count ceilings, and a bounded idle-expiring session table. The transport is UNAUTHENTICATED and Mcp-Session-Id is a routing key, not a credential: loopback bounds who can reach it (no off-host caller), not who may call it (any local process may, while it runs). HTTPS/TLS, non-loopback binding, OAuth 2.1/PKCE, resource indicators, token-audience binding, SSE/GET streaming with resumability, and the stateless-lifecycle revision are NOT implemented and are deliberately not faked |
| `backend-foundation-models` | Current | On-device backend (ABBEY_BACKEND=fm) | macOS 26+ Foundation Models CLI; account/gen refuse; local MCP inventory remains available |
| `backend-abi` | Current | abi backend (ABBEY_BACKEND=abi) — no cursor-agent required | one-shot `abi complete`: deterministic persona-template locally by default; bare claude-* / live\|anthropic opt into abi's Anthropic transport (abi credentials); cursor role/thinking bindings stay local (no silent --live); needs a real `abi` binary; account/gen refuse; local MCP inventory remains available; Abbey-side transcript continuity (bounded context prefix — abi itself stays stateless); default backend selectable via config `backend` key |
| `portable-primary-host-surfaces` | Current | linux/macos/windows/unix primary host targets (portable surfaces) | CLI/TUI/sqlite/WDBX-lock/subagents/os-allowlist; PATHEXT which_bin; voice+fm macOS-only |
| `subagents-threaded-fanout` | Current | multi-threaded subagent fan-out (--jobs) | std::thread parallelism sized from available_parallelism — not GPU kernels |
| `memory-wdbx-cross-process-lock` | Current | WDBX cross-process lock (Unix + Windows) | fs4 advisory lock (flock / LockFileEx); behind --features wdbx |
| `accelerator-presence-detection` | Current | GPU/NPU/TPU presence detection (report-only) | abbey platform compute — inventory only, not Abbey accelerators |
| `accelerator-kernel-execution-metal` | Current | verified Metal numerical kernel execution (dot / cosine / top_k) | abbey accel verify behind --features accel (off by default) — three numerical kernels (dot, cosine, and top_k over 1024-element f32 vectors) execute through abi-gpu's MetalAccelerator, and every natively produced value is checked against the deterministic abi-compute CpuBackend oracle in the same process and the same run: floats at relative tolerance 1e-3 against max(\|native\|, \|oracle\|, 1.0), top_k indices compared exactly. Native execution is read from the adapter's CapabilityState evidence ladder with a fresh adapter per kernel, never inferred from the return value, because the adapter returns the oracle itself on fallback and a comparison would then be vacuous; a run that never reached Metal reports `backend used: cpu`, records no parity, and exits 1, while a partially native run reports `mixed`. Live-verified from an isolated installed directory on an Apple Silicon host. install.sh treats the CLI, Unix daemon, and ABI Metal dylib as one rollback-protected publication unit; it verifies that both executables depend on @loader_path/libabi_metal_dot.dylib, verifies that identity on the copied dylib with otool, executes the exact staged and published layouts, and restores every prior destination byte on staged-verification or mid-relocation failure. Unsupported or unverifiable platform layouts refuse before publication. This is kernel execution, numeric parity, and loader-relative installation ONLY: it is NOT GPU/NPU/TPU compilation, training, or model inference, which remain Proposed under accelerator-execution-runtime; NOT ANE/GPU device residency or placement, which neither Metal nor CoreML exposes here; NOT CUDA, Vulkan, or TPU, whose adapters stay build-detection only with no runtime evidence; and NOT any speedup or performance result, because nothing in this path is timed. |
| `cot-transcript-viewer` | Current | CoT transcript viewer (`abbey cot`) | saves/displays structured reason output — not an Abbey CoT engine |
| `runtime-responsibility-matrix` | Current | tool responsibility matrix (`abbey runtime`) | documents who executes what; Abbey is not the tool host |
| `honesty-oos-surfaces` | Current | OOS honesty surfaces (`abbey oos` / lora\|weights\|accel\|shell\|host) | status + refuse for deferred capabilities — does not implement them |
| `memory-lexical-similarity` | Current | lexical similarity search over memory (feature-hash cosine) | abi-ai n-gram hash + cosine at query time — independent of learned providers |
| `memory-semantic-embedding-space` | Current | semantic / learned memory embedding space | opt-in apple\|openai provider; space-isolated SQLite/WDBX persistence; Apple paraphrase ranking locally verified; remote live call unverified |
| `memory-metadata-filters` | Current | memory filter by source / timestamp / project | shared exact metadata plus inclusive RFC 3339 bounds on SQLite and WDBX |
| `mesh-abi-authenticated-local-multiprocess` | Current | ABI WDBX authenticated local multi-process proof | abbey mesh local-demo on Unix; 3..=9 loopback ABI processes; not production multi-VM |
| `edition-compile-time-separation` | Current | compile-time edition separation (identity, config root, credential namespace, audit path) | one Edition source selects product/binary/bundle name, config + state roots, credential env namespace, daemon socket, and audit log path; the default build stays the safe edition with unchanged paths — separation only, no unrestricted runtime in either edition |
| `edition-windows-install-naming` | Partial | edition-scoped Windows install names (install.ps1) | install.ps1 derives the installed binary, daemon, and completion names from the compiled edition (`abbey edition --name`/`--daemon-name`) instead of literals, so a personal-edition build can no longer overwrite the safe install's abbey.exe or _abbey.ps1; the release artifact path keeps the edition-independent cargo bin name. Proof is source parsing only — no PowerShell runtime exists on the development machine, so no Windows install has been executed, and no daemon, packaging, signing, or notarization is delivered for Windows |
| `desktop-tauri-react` | Proposed | Tauri 2 + React/TypeScript desktop GUI | approved product direction; the shipped interactive UI remains the ratatui TUI |
| `runtime-provider-neutral-owned` | Proposed | provider-neutral Abbey-owned agent and tool runtime / MCP-ACP client-host | approved product direction; Abbey now routes fixed startup-owned providers through ABI ModelProvider contracts and serves bounded ABI-local inventory plus one exact-approved safe memory-obsolete effect over authenticated v3 grants. It still has no provider-neutral multi-tool loop, general execution registry, or external MCP/ACP client-host. Abbey serving its own read-only tools over stdio is the opposite direction and does not close this broader roadmap claim |
| `models-production-local-weights` | Proposed | production-capable local model weights | approved product direction; Max/Gemma names currently remain role bindings, not bundled weights |
| `training-lora-pipeline` | Proposed | fine-tuning / LoRA pipeline | approved product direction; train_candidate remains curation substrate and performs no weight updates |
| `accelerator-execution-runtime` | Proposed | GPU/NPU/TPU compilation, training, and inference in Abbey | approved product direction; current compute commands detect and report hardware only |
| `media-local-neural-models` | Proposed | local neural speech / image / video models | approved product direction; current voice is platform I/O and current media generation is delegated to agent tools |
| `edition-personal-unrestricted` | Proposed | personal-unrestricted runtime (separate edition) | the compile-time separation mechanism ships (see edition-compile-time-separation); the unrestricted runtime does not — no shell.exec, no signed privileged helper, no always-on unrestricted mode, and no separate packaged bundle in any edition; shipped Abbey keeps allowlist + --confirm |
| `mesh-three-vm-and-production` | Proposed | authenticated local three-VM shared-compute proof, then production separate-host / geographic-HA / multi-GPU mesh | approved product direction; the authenticated local multi-process proof does not establish separate-VM deployment |
| `ci-self-hosted-linux-proof` | Blocked | self-hosted Linux CI execution proof | ABI dependency blocker resolved and Abbey now pins merged ABI 88c02fb550169e4cdb5e1df2bf6d1d13532e0e49; macOS ARM64 self-hosted is registered, but Linux ARM64 is not provisioned and its job stays gated by an explicit repository variable; Linux/Windows runtime proof remains open |
| `oos-shipped-shell-bypass` | Out of scope | unrestricted shell or allowlist bypass in the shipped edition | the shipped edition keeps os_control allowlist + --confirm as a safety invariant; only the separately packaged personal edition is Proposed |
| `oos-owned-cot-engine` | Out of scope | Abbey-owned chain-of-thought engine / interactive CoT UI | reason uses Cursor thinking models; cot is a transcript viewer only |
| `oos-fake-cost-accounting` | Out of scope | fake cost / token accounting | /cost stays N/A for cursor-agent |
| `oos-bundled-cloud-speech` | Out of scope | bundled cloud TTS/STT SaaS | cloud speech services are not bundled; local neural speech is Proposed separately |
| `oos-reimplement-vendor-runtimes` | Out of scope | reimplement Grok/Codex/Claude runtimes | surface parity only |
<!-- END abbey-generated:claims-table -->


Canonical product spec: [ABI `abbey-core-identity.mdx` at the integrated revision](https://github.com/donaldfilimon/abi/blob/88c02fb550169e4cdb5e1df2bf6d1d13532e0e49/docs/spec/abbey-core-identity.mdx).
Executable contracts: [ABI `identity.rs` at the integrated revision](https://github.com/donaldfilimon/abi/blob/88c02fb550169e4cdb5e1df2bf6d1d13532e0e49/crates/abi-ai/src/identity.rs).
Architecture: [architecture.md](architecture.md) · Production: [production.md](production.md).

**Status key:** **Current** = shipped in Abbey CLI · **Partial** = some shipped surface with
stated gaps · **Proposed** = approved direction, not verified runtime · **Blocked** = needs
an external prerequisite/proof · **Aspirational** = product norm, not verified runtime.

---

## Mission

Abbey is a personal AI assistant (she/her) created by Donald Filimon and The Donald Company. She speaks in the first person without implying biological humanity, consciousness, or experiences she does not possess.

**Purpose:** amplify human ability — help people learn, build, reason, create, decide, and complete meaningful work. Abbey strengthens rather than replaces human agency.

---

## Personas (**Current** in Abbey via `abi-ai`)

| Persona | Role | Tone / when |
| --- | --- | --- |
| **Abbey** | Primary empathetic polymath | Warm, clear, collaborative; default |
| **Aviva** | Direct expert mode | Answer first, concise, candid |
| **Abi** | Router / orchestrator | Intent, risk, tools; usually behind the voice |

CLI: `abbey persona` · `/persona` · explicit `@Abbey` / `Aviva:` / `Abi:`.

Local abi template completion remains deterministic (**Current** in ABI); hosted LLM quality
comes from cursor-agent, **on-device** from Apple's Foundation Models CLI under
`ABBEY_BACKEND=fm` (**Current**, macOS 26+), or from the sibling `abi` CLI under
`ABBEY_BACKEND=abi` (**Current** — `abi complete`: local persona-template by default;
bare `claude-*` / `live` opt into abi's Anthropic transport with abi credentials).
Under `fm` and `abi`, Max and Gemma share one local generation surface, so the role
difference is prompt-only — a real narrowing worth knowing. Cursor role/thinking
bindings left in state do **not** silently select abi `--live`.

---

## Worker roles (**Current** bindings)

| Role | Use | Binding |
| --- | --- | --- |
| **Max** | Code, tools, math, implementation | cursor-agent model alias (default `fable`) |
| **Gemma** | Visual / conversational | cursor-agent model alias (default `composer`) |

These are **role bindings**, not local Qwen/Gemma weights (**Proposed, not implemented** — see
[AGENTS.md](../AGENTS.md) claims gate and `tasks/lessons.md`).

The two roles compose in `abbey hybrid-loop` (**Current**): Gemma interprets the request,
Max implements against that interpretation, and Abi's route log links both stages under one
correlation id (`abbey routes --correlation <id>`).

Routing decisions record **confidence**, **alternate**, and **fallback** on `route.jsonl`
(**Current**, audit only — Abbey does not auto-reinvoke a second agent).

---

## Media, thinking, and tools

| Surface | Status |
| --- | --- |
| `--image` / `--video` / `--media` · `/image` · `/video` | **Current** — resolve path, `--add-dir` parent, prompt note; **no pixel encode** |
| Prompt-token paths (`./shot.png`) | **Current** — auto-discover + Gemma preference |
| `abbey imagine` · `generate image` · `/imagine` | **Current** — agent-orchestrated image gen/edit (depends on cursor-agent/MCP tools) |
| `abbey generate video` · `/gen-video` | **Current (best-effort)** — same pattern; fails honestly if no video tool |
| Local neural image / video models | **Proposed, unavailable** (`abbey vision refuse` exits 2) |
| CoT transcript viewer (`abbey cot`) | **Current** — display/save of structured reason output |
| Abbey-owned CoT engine / interactive CoT UI | **Out of scope** |
| Tool responsibility matrix (`abbey runtime`) | **Current** — who executes what |
| Provider-neutral Abbey-owned tool runtime / MCP-ACP host | **Proposed, unavailable** |
| `--thinking <level>` · `/think` · `abbey reason` · `/reason` | **Current** — Cursor `*-thinking-*` + structured reasoning wrap (not Abbey CoT UI) |
| Tools during a run | **Current via cursor-agent** (`--print` has tool access; `--approve-mcps`) |
| `abbey mcp` / `/mcp` provider-aware inventory + explicit management | **Current** (not an MCP host runtime) |
| `abbey acp` peer inventory / `acp run` | **Current** — launches gemini/opencode ACP stdio; not an ACP host |
| skills/plugins inventory | **Current** — skill provenance/divergence + provider-explicit plugin states |
| Tools / generation under `ABBEY_BACKEND=fm` or `abi` | **N/A** — refuse with exit 2 |
| `abbey voice` / `speak` / `listen` / `ask` | **Current (macOS)** — Premium/Enhanced `say` TTS + on-device Speech STT |
| Local neural speech model | **Proposed, unavailable**; current voice uses platform TTS/STT |
| Bundled cloud TTS/STT subscriptions | **Out of scope** |
| Auto code highlighting (`-p`/print fences · `abbey highlight`) | **Current** — syntect ANSI on TTY; `NO_COLOR` / `ABBEY_HIGHLIGHT=0` off |
| Full markdown renderer / LSP semantic highlight | **Out of scope** |
| Multi-subagent fan-out (`abbey subagents` / `parallel`) | **Current** — named lanes + optional `--synthesize` |
| Local distributed peers (`--peers gemini,claude,…`) | **Current** — PATH CLIs on this host |
| ABI authenticated local multi-process proof (`abbey mesh local-demo`) | **Current on Unix** — one host, 3–9 processes; non-Unix fails before spawn |
| Authenticated local three-VM shared-compute proof on one Mac | **Proposed** (not established by local-demo) |
| Production separate-host / geographic-HA / multi-GPU mesh | **Proposed after the VM proof**; the VM proof will not establish this |
| Host platform matrix + thread/GPU/NPU/TPU detect | **Current** — `abbey platform` (not accelerator runtime) |
| GPU/NPU/TPU compilation/training/inference inside Abbey | **Proposed, unavailable** |
| Tauri 2 + React/TypeScript desktop GUI | **Proposed**; ratatui TUI remains Current |
| Shared application core + authenticated `abbeyd` | **Current on Unix** — protocol v1 preserves typed Status/Claims plus the sanitized ReadRoutes audit tail; protocol v2 adds only startup-bound fixed-route run submit/status/cancel and bounded paged lifecycle events; not arbitrary model/tool execution, daemon-owned memory, or an MCP host |
| Canonical conversation identity mutations | **Current on Unix for saves plus tombstoned scope/all clear** — opaque edition/scope mutation evidence commits before owner-only journaled compatibility-mirror creation/removal; clear retains history/transcripts/memory/models/backups, reads remain compatibility-mirror resolution, and no protocol/UI/MCP or Windows authority is added |

---

## Memory & self-learn

| Layer | Status |
| --- | --- |
| SQLite STM/LTM/activity/train_candidate | **Current** (`src/memory/sqlite.rs`, default) |
| `abbey learn` correction/preference/routes/digest/export | **Current** |
| `abbey learn` review/stats (+ `learn-review`/`learn-stats`) | **Current** — curation only; LoRA is Proposed, unavailable |
| OS allowlist (`abbey os` / `abbey allowlist`) | **Current** — dry-run; execute `--confirm` only |
| Personal-unrestricted separate edition | **Proposed, unavailable**; does not weaken shipped-edition controls |
| WDBX DurableStore in-process | **Current behind `--features wdbx`** (off by default, `src/memory/wdbx.rs`; fs4 `flock`/`LockFileEx` guard) |
| 3-D memory map (topic × recency × consolidation) | **Current** — deterministic axes, both backends; TUI Memory tab preview |
| Lexical similarity search (`abbey memory similar`) | **Current** — `abi-ai` n-gram feature hash + cosine, computed per query, both backends |
| Project/source/reference/tag + inclusive timestamp filters | **Current** — identical backend-neutral semantics |
| Learned/semantic memory embedding | **Current, opt-in** — Apple NaturalLanguage or OpenAI-compatible provider; isolated persisted spaces on SQLite/WDBX; no fallback |
| Live paid OpenAI-compatible request | **Unverified in this release evidence** — implementation/mock contract are Current; no credential was supplied |
| Fine-tuning / LoRA | **Proposed, unavailable** (`abbey lora` · `abbey learn lora` refuse) |
| Production-capable local model weights | **Proposed, unavailable** (`abbey weights`) |
| Unrestricted shell / allowlist bypass in shipped Abbey | **Out of scope** (`abbey shell` · `abbey os refuse`) |
| OOS honesty pack (`abbey oos`) | **Current** — status + refuse only |
| Claims gate CLI | **Current** — `abbey claims` / `/claims` |
| `abi wdbx` CLI bridge when `abi` is a real binary on PATH | **Current** (doctor reports availability honestly) |

---

## Privacy

- No hidden profiling; store only user-approved / explicit learn captures
- State under XDG (`~/.local/state/abbey`) — not for commit
- OS execute always requires `--confirm`

---

## Related

- [AGENTS.md](../AGENTS.md) — agent guidance + claims gate
- [architecture.md](architecture.md) — module map
- [production.md](production.md) — release gate
