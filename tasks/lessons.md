# Lessons

<!-- BEGIN abbey-generated:claims-summary -->
<!-- Generated by tools/check_claims_sync.py; do not edit. -->
Canonical capability ledger: **37 Current · 2 Partial · 8 Proposed · 1 Blocked · 5 Out of scope** ([full evidence](../docs/claims.md)). Source: `src/claims.rs`; schema 1; digest `fada0a934a47dad9ed1ccd723995102447e2d3e739bd7fbce56c72fcec3290cb`.
Executable workflow ledger: **26 goals (23 done, 1 in_progress, 1 proposed, 1 blocked) · 164 checked / 22 open todos** (stable goal metadata in `tasks/goals.md`).
<!-- END abbey-generated:claims-summary -->


- Abbey `/init` should stay offline-first (filesystem scan); `--agent` is optional refinement only.
- Keep parity claims honest: cursor-agent is the default backend; `/cost` N/A; Max/Gemma = bindings.
- Goals live in `tasks/goals.md`; granular work in `tasks/todo.md`.
- **Max/Gemma worker roles are cursor-agent model bindings**, not local Qwen or Gemma checkpoints — naming a role after a model family does not imply in-process weights or LoRA; document and doctor output must say which hosted id each role maps to.
- Under `ABBEY_BACKEND=abi`, never let a cursor `claude-*-thinking-*` (or Max/Gemma leftover) silently select `--live` — normalize at the argv choke point.
- Route **confidence / alternate / fallback** are audit fields only — do not add a second agent execution path when confidence is low; keep the one canonical `run_agent` path.
- please-fix must summarize captures before argv handoff; bare `running `/`using `/`reading ` are not always noise (cargo output is useful).
- `learn review`/`stats` are provenance curation, not a trainer. LoRA is now an approved
  Proposed direction, but approval does not turn curation into weight updates; keep refusal
  exit 2 until training, evaluation, rollback, and artifact provenance are real.
- Voice “super high quality” on macOS means **downloaded Premium/Enhanced `say` voices** + on-device Speech STT — not a cloud subscription and not Abbey-owned neural weights.
- **A process-cached value must not gate per-call behaviour that a runtime switch can change.** `AgentBackend::from_env()` is resolved once; the TUI's Ctrl-B switch mutates `AgentConfig::backend`. `read_chat` gated its `CURSOR_AGENT_CHAT_ID` guard on the cached value, so switching to `abi` mid-session silently resumed a cursor chat id and killed continuity — the very bug the guard had just been added to fix. Thread the live backend (`read_chat_for(cfg.backend)`).
- **Fixing a perf problem can reintroduce a correctness one.** The `OnceLock` on `from_env()` was added to stop per-frame `config.toml` reads in the TUI draw path; it was correct for that, and wrong for the guard that happened to share the call. Check every caller when you memoize something.
- **A commented example must be a clean `key = "value"`, never prose that starts like one.** The scaffold's `abi_bin` note began `# backend = "abi" and for …`, so uncommenting the real `backend` line matched two lines and parsed prose as the value. Pinned by a test that walks `default_toml_text()`.
- **Silent fallback hides a config typo.** `backend = "abbi"` used to mean "cursor-agent, no explanation". Unknown values now warn — a user who names an executor should never be quietly given a different one.
- **A caveat repeated across docs can be wrong in all of them.** README/architecture/production described the WDBX lock as `fs2` (and README as Unix-only) long after the code moved to `fs4` with `LockFileEx` on Windows. Grep the crate name in `Cargo.toml`, not the other docs.
- **A scope decision changes the roadmap status, not the evidence status.** Moving GUI,
  Tauri 2 + React/TypeScript desktop, owned runtime/tool hosting, local weights, LoRA, accelerator execution, neural media,
  a separate personal-unrestricted edition, and multi-VM compute from Out of scope to
  Proposed must not remove fail-closed refusal paths or promote them to Current.
- **Runner registration and CI execution are different facts.** GitHub-hosted
  `startup_failure`, a registered macOS ARM64 self-hosted runner, an unprovisioned
  variable-gated Linux ARM64 job, and open Windows runtime proof must be reported
  separately. A merged ABI dependency revision removes one blocker; it does not prove
  another platform ran.
- **Owned runtime work should reuse ABI boundaries, not fork them.** Establish
  provider-neutral executor/tool/compute contracts first, then layer GUI, local models,
  accelerators, training, neural media, and multi-VM scheduling over those contracts.
  Vendor runtime reimplementation remains Out of scope.
- **Credential policy must name the intentional reader.** Executor credentials belong to
  their backend, while the opt-in OpenAI-compatible embedding provider intentionally reads
  an environment key. The security invariant is no persistence, logs, config storage, or
  argv exposure—not the impossible claim that Abbey never reads a credential.
- **A second Cargo binary makes implicit package commands ambiguous.** Once `abbeyd`
  joined `abbey`, the claims validator's bare `cargo run` stopped selecting a target.
  Set `default-run = "abbey"`, and make automation/CI name `--bin abbey` explicitly;
  human convenience and reproducible automation are separate concerns.
- **A shared contract is not an owned runtime.** The Current app-core/daemon slice
  advertises only Status and Claims. Defining approval or run identifiers does not
  grant execution capability, and a Unix socket does not prove durable jobs, model
  workers, MCP hosting, memory ownership, or Windows named-pipe support. A client must
  correlate request and event kinds, validate protocol/schema/capability invariants,
  and keep bearer material out of Debug, errors, output, persistence, and argv; adding
  that client does not broaden daemon authority.
- **Pin sibling-repo dependencies by immutable commit in CI.** A CI or release build
  that checks out a floating sibling branch (ABI `main`) is not reproducible — record
  the exact merged SHA and check that out, so the same Abbey revision always builds
  against the same ABI revision.
- **Source compilation, local runtime proof, hosted CI, and external audit are separate
  evidence classes.** A green local `./check.sh`, a live run on this Mac, a successful
  hosted job, and an independent audit each prove something the others do not; never
  let one class stand in for another in a claim.
- **Claims must be generated or validated from a single source, never manually
  duplicated.** Every hand-copied claim table is a future drift site; sync docs,
  ledgers, and CLI output from the canonical `src/claims.rs` ledger via a checked
  generator/validator.
- **A model-produced structured tool call is the execution boundary.** Raw retrieved or
  user-supplied text is never passed directly to a shell; only a validated, schema-typed
  tool call the model explicitly emitted may cross into execution.
- **Public-safe and personal-unrestricted editions need separate binaries, bundle IDs,
  config roots, and tool registries.** A runtime flag inside one binary is a bypass
  waiting to be flipped; edition separation is a packaging boundary, not a config value.
- **A three-VM demo on one Mac is multi-VM evidence, not physical multi-host evidence.**
  It proves identity/quorum/lease mechanics across VM boundaries; separate-physical-host,
  geographic-HA, and real network-partition behavior remain unproven until run on real
  separate machines.
- **Gemma E4B adapters are not transferable to Gemma 12B.** LoRA adapters are tied to the
  exact base architecture and dimensionality they were trained against; a different
  parameter-count sibling model needs its own adapter training and evaluation.
- **CoreML output-oracle success proves execution under requested compute units, not ANE
  placement or residency.** Matching outputs with `.all`/`.cpuAndNeuralEngine` shows the
  graph ran and agreed numerically; it does not show which unit executed any layer —
  placement claims need profiler/instrument evidence.
- **Optional TFHE-rs integration is not an independent cryptographic audit.** Wiring in a
  reviewed library says nothing about our parameter choices, key handling, or protocol
  composition; "audited dependency" and "audited system" are different claims.
- **The component that validates a request must also derive its idempotency digest.** A
  caller-supplied hash is merely another assertion and can make different requests look
  identical. Serialize the validated typed request canonically and bind its key to that
  digest inside the manager.
- **Durable state and execution authority are separate surfaces.** Publishing lifecycle
  contracts and a tested SQLite run manager does not add an `AppCommand`, daemon
  capability, or provider process. Promote the command surface only after protocol
  versioning, bounded capture, cancellation, and descendant teardown are proven.
- **Restart recovery must never replay an ambiguous external action.** Keep queued work
  durable, but mark Starting/Running/CancelRequested runs Interrupted after restart. An
  explicit retry can reuse the request identity safely; automatic replay could duplicate
  a filesystem mutation or tool call that completed just before the crash.
- **A terminal decision is not durable until its compare-and-swap transition commits.**
  Completion and cancellation can both observe `Running`; if cancellation publishes
  `CancelRequested` first, the worker must reload state and converge to `Cancelled`
  instead of discarding `UnexpectedStatus`. Keep the run admitted until a terminal row is
  visible, and pin the interleaving with a deterministic barrier test rather than hoping
  a timing stress test happens to find it.
- **A process leader exiting does not prove its capture pipes reached EOF.** Descendants
  can inherit stdout/stderr and outlive the leader, so join-only capture can hang forever.
  Keep process-group ownership until both the leader and descendants are gone; terminate
  the group before joining readers, and retain a drop guard for every error path.
- **A reaped process group and a scheduled reader message are separate clocks.** After
  group teardown proves no owned writer survives, a loaded test process may still delay
  the capture threads. Give collection its own hard-capped scheduling window; reusing the
  signal grace turns scheduler latency into a false open-pipe failure.
- **Split a near-cap fixture module before the next behavior lands.** Moving existing
  protocol-family tests first keeps the extraction review mechanical and leaves the
  following authority PR's diff attributable to the new command rather than file pressure.
- **A generic supervisor can remain private while adapters define product authority.**
  Abbey's supervisor accepts an internal process specification for reuse and testing, but
  the public delegated executor exposes only fixed startup-bound recipes. Requests never
  choose a binary, argv prefix, environment, workspace, or shell, and diagnostics report
  counts/stable codes instead of prompts, output, paths, or secret-bearing environment.
- **Protocol downgrade is safe only for replay-safe reads.** A client may retry Status or
  Claims once after an explicit `unsupported_version`, but must never automatically
  replay SubmitRun or CancelRun: a lost response does not prove the mutation did not
  happen.
- **A new protocol type is not daemon authority.** Keep v3 commands, events, grants, and
  negotiation in a separate serialized family until the authenticated transport and each
  handler exist. Leaving the daemon's supported list at `[1, 2]` makes a contracts-only
  slice observable as an explicit refusal instead of silently widening v2.
- **Approval authority must bind the canonical call, not its display text.** A typed v3
  approval carries the exact lowercase SHA-256 call digest and a single decision ID;
  summaries remain presentation data and cannot authorize a different tool input.
- **Paged lifecycle history needs a fixed first-page watermark.** Capture the high-water
  sequence on page one, require it on continuations, and fail closed on gaps, future
  cursors, or state/event disagreement. Raw prompts and provider bytes belong in neither
  snapshots nor lifecycle events.
- **Advertised runtime capability must derive from startup-owned authority.** With no
  configured ABI-local or Foundation Models route, protocol v2 omits only SubmitRun;
  durable read, event, cancellation, and reopen operations remain available without
  inventing an executable from request data.
- **A shared reducer creates presentation parity, not new daemon authority.** CLI and
  TUI should emit the same typed `AppCommand` and consume the same sanitized `AppEvent`
  projection instead of reimplementing transport, downgrade, lifecycle, or execution
  policy.
- **Snapshots and fixed-watermark pages are different evidence.** A later status may
  legitimately skip intermediate states, while contiguous event pages must follow every
  legal transition without sequence gaps, watermark drift, or post-terminal mutation.
- **Frontend-neutral does not mean every frontend ships the capability.** CLI/TUI proof
  does not widen the Tauri invoke allowlist; every bridge needs explicit wiring and its
  own runtime evidence.
- **Render only the sanitized reducer state.** Human and JSON output must derive from the
  validated projection, never the original prompt, provider bytes, bearer, or bound
  executable path.
- **Preserve before importing legacy metadata.** Metadata migration must preserve and
  hash the exact legacy source before committing its database marker; missing backend,
  title, transcript, memory, or run evidence must remain unknown rather than being
  inferred. Filesystem and SQLite state cannot share one transaction, so finalize and
  verify a content-addressed backup first, then use one immediate database transaction;
  neither the original source nor a finalized backup may be deleted automatically.
- **Compatibility mirrors are projections, not co-authoritative state.** Commit the
  opaque canonical identity and mutation-token digest to `runtime.sqlite` first, then
  complete the exact edition-scoped mirror plan through one owner-only locked journal.
- **Cross-store recovery must distinguish prepared from committed work.** Discard a
  prepared-but-uncommitted plan, idempotently finish an exactly marked committed plan,
  and compare before/after history digests so replay never duplicates an entry or
  overwrites divergent data.
- **A logical identity clear is a canonical tombstone, not an uncoordinated file
  deletion.** Commit the exact scope/all mutation first, then let the sole locked journal
  remove only destinations whose authenticated plan and before-state still match.
- **Prepared and committed clears recover differently.** Discard an uncommitted plan
  without removal; replay an exactly marked committed or partially applied plan
  idempotently; refuse missing effects, changed targets, and incomplete inventories.
- **Identity clear is not user-data cleanup.** Clearing compatibility identity mirrors
  must retain conversation provenance, history, transcripts, semantic memory, models,
  route/run data, and finalized migration backups.
- **Scope clear, clear-all, and read fallback are separate semantics.** A per-cwd clear
  preserves the global fallback, an edition clear-all covers only a bounded exact mirror
  inventory, and tombstones prevent stale resurrection; none of that proves canonical
  `read_chat_for` selection.
- **Canonical selection must fail before provider side effects.** Resolve the
  edition-scoped cwd/global scope, tombstone, and selected-mirror digest through the
  fallible path before create, resume, retry, or capture. A lossy presentation wrapper is
  safe only when it cannot turn corruption into an executing fallback.
- **Save and clear share one serialization and revision boundary.** Concurrent mutation,
  history observation, compaction, and legacy capture must all pass through the same
  owner-only journal lock and monotonic canonical receipt history.
- **Metadata-only access must not trigger unrelated lifecycle recovery.** Identity
  coordination opens schema metadata without rewriting interrupted runs; sharing a
  database file does not merge those authorities.
- **A test that passes alone and fails in the suite is an isolation defect, not noise.**
  `conversation_identity_cutover`'s failpoint-recovery test failed once under the full
  gate and passed standalone on the same commit. The instinct is to re-run until green;
  the discipline is to record it, because the flake is evidence the test depends on
  state it does not own. Prove causation before blaming a branch: check whether the
  suspect commits touch the subject code at all — the branch that surfaced this one
  contained no Rust changes.
- **A green `./check.sh` cannot see a check that cannot fail.** Across one Phase 2
  slice the gate passed on every commit while review found: a permissions test that
  passed with a `contents: write` escalation present, its fix reading only the first
  line so `contents: read` + `pull-requests: write` slipped through, and an evidence
  predicate reporting success for a run still executing. Each was caught by demanding
  an empirical fail-then-pass demonstration, never by the suite going green.
- **Writing complete code into a plan moves where the bug enters; it does not remove it.**
  Three of that slice's five findings were defects in the plan text itself, transcribed
  faithfully by implementers. Plan-supplied code needs the same red-green proof as
  hand-written code — especially when it encodes an external system's semantics, such as
  GitHub Actions job-level `permissions:` fully overriding the workflow default rather
  than intersecting with it.
- **A blocking provider needs a checkpoint, not the cancellation token.** Exposing the
  token lets an adapter manufacture cancellation; emitting dummy events corrupts event
  and budget accounting. A read-only `RunContext::checkpoint` lets the existing bounded
  process supervisor poll cancellation and duration budgets while startup-owned recipes
  retain all executable, argv, environment, workspace, model, and teardown authority.
- **A new protocol family needs a new envelope, not a wider legacy enum.** Decoding v3
  through the v1/v2 `AppCommand` envelope would make old clients and downgrade behavior
  ambiguous. A separate strict envelope preserves byte fixtures and makes schema/grant
  validation happen before dispatch.
- **Negotiation is not authority unless the command carries and the daemon rechecks the
  exact grant.** Require a canonical echoed grant on every non-negotiation request, reject
  a missing grant before the handler, and still derive real authority from startup-owned
  provider objects so a forged unsupported grant cannot manufacture a route.
- **Advertise and execute from one provider object.** Deriving v3 model inventory and v2
  execution routes from the same frozen `ModelProviderExecutor` prevents documentation or
  presentation state from inventing a model that the daemon cannot actually route.
- **A negotiated client session should own the returned grant set.** Letting each caller
  rebuild grants turns an authenticated negotiation into presentation advice. Keep the
  validated set private in the session and echo it unchanged on every typed follow-up.
- **Retry policy belongs to a protocol family, not a shared socket helper.** Reusing the
  bounded transport is safe; inheriting v1 read downgrade is not. Protocol-v3 negotiation
  and reads each make one correlated request so later mutations cannot acquire replay by
  accident.
- **A stable claim ID is an authority boundary, not a search hint.** Match the canonical
  registry by exact equality, return one stable `not_found` result for missing or partial
  IDs, and require the typed client to correlate the response ID with its request. Fuzzy
  lookup or filtering needs a different capability and bounded result contract.
- **Inventory should project one safe registry, not copy it.** Derive protocol-v3 tool IDs,
  descriptions, and input schemas from the same `SAFE_TOOLS` objects served by MCP, validate
  the projection at daemon startup, and leave handlers out. A `list_tools` grant then proves
  discoverability only; it cannot accidentally become invocation or approval authority.
- **Extract a near-cap authority module before adding behavior.** Moving the typed v3
  session reads unchanged at 780 lines leaves a reviewable 637-line transport/client core
  and a 152-line v3 module. That keeps the next invocation review about authority and
  policy instead of hiding it inside a mixed refactor.
- **Persist authorization before even read-only execution.** A protocol grant is only the
  outer boundary: resolve the startup registry entry, compile and validate its exact schema,
  reserve the caller's ID, persist a digest-only policy decision, then execute through the
  shared ABI contract. Recovering those reservations from the audit on reopen makes duplicate
  handling deterministic without retaining raw inputs or outputs.
- **A local pending mutation must not widen a shared unauthenticated registry.** Keep MCP's
  structurally read-only `SAFE_TOOLS` unchanged, append the mutating descriptor only inside
  the authenticated default daemon, validate before policy, persist only the exact-call
  digest, and return pending without retaining input or executing. Discoverability,
  approval decisions, consumption, and execution are separate authority reviews.
- **Approval state is product authority even before execution exists.** Negotiate it only
  in the default authenticated daemon, bind every decision to the stored call digest and
  a globally unique resolution ID, return raced expiry as data, and keep approval,
  consumption, and execution as separate transitions. The personal and MCP catalogs must
  not inherit decision authority merely because they share protocol contracts.
- **Cancellation is a terminal authorization transition, not an execution shortcut.**
  Resolve the exact durable call ID, share the global resolution-ID namespace with
  decisions, permit only pending or approved records, and return raced expiry as typed
  state. A cancelled approval must remain unconsumed and must never trigger the effect.
- **Consumption without durable intent creates an unclassifiable crash window.** Atomically
  consume the exact approval and persist prepared intent before touching the effect, retain
  only terminal digests, and recover unfinished intent as interrupted. Ambiguous mutations
  require a fresh explicitly approved call rather than replaying the consumed call.
- **Approval is not implicit permission to execute later without a new protocol action.**
  Require the caller to resubmit the identical digest-bound call, atomically consume approval
  with prepared intent before the effect, and fail closed on every changed or terminal call.
  Exercise process exits both after prepare and after the effect: reopen must classify either
  window as interrupted, never invent a result digest, and require a fresh call and approval
  even when the idempotent effect may already have completed.
- **A memory read projection must constrain both matching and serialization.** Searching
  raw payload or provenance and redacting only the returned label still leaks that hidden
  data through result membership. Search the same sanitized summary that crosses the wire,
  replace internal IDs with domain-separated digests, and bind each bounded snapshot
  watermark to the exact query so concurrent writes and cross-query cursor reuse cannot
  reinterpret a page.
- **A loaded model is process state, not a durable routing claim.** Persist lifecycle
  operation evidence and fail ambiguous queued/running work on reopen, but require an
  explicit reload after restart and keep the provider out of global/default routes until
  a separately reviewed exact-model inference command exists. Signed manifests, accepted
  licenses, verified artifacts, and initialized devices are prerequisites; none alone is
  evidence that inference executed.
