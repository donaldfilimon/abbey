# AGENTS.md

<!-- BEGIN abbey-generated:claims-table -->
<!-- Generated by tools/check_claims_sync.py; do not edit. -->
Canonical capability ledger: **40 Current · 3 Partial · 8 Proposed · 1 Blocked · 5 Out of scope** ([full evidence](docs/claims.md)). Source: `src/claims.rs`; schema 2; digest `438b21e0ee9376aa2a09d3dc013f87eab152594a227ab3bd8736a51dbd4e6507`.
Executable workflow ledger: **27 goals (24 done, 1 in_progress, 1 proposed, 1 blocked) · 168 checked / 25 open todos** (stable goal metadata in `tasks/goals.md`).

| Stable ID | Status | Capability | Evidence boundary |
| --- | --- | --- | --- |
| `backend-ollama` | Current | preferred ollama backend with local gemma4:26b-mlx default | preferred default, never required — the unchosen default uses ollama when installed, else grok\|fm\|abi\|claude then cursor last; local verbs need no executor; ABBEY_BACKEND=ollama\|cursor\|grok\|fm\|abi\|claude selects explicitly; default model gemma4:26b-mlx (alias gemma:27b-mlx); not bundled Abbey weights |
| `backend-cursor-agent` | Current | optional cursor-agent backend | explicit ABBEY_BACKEND=cursor only — not the unchosen default and never required; local verbs need no executor; cursor remains a last-resort auto fallback after ollama\|grok\|fm\|abi\|claude |
| `surface-parity-grok-codex-claude` | Partial | Grok/Codex/Claude surface parity | selected UX aliases and compatible surfaces ship; parity polish continues; not a reimplementation of vendor runtimes |
| `personas-abbey-aviva-abi` | Current | Persona Abbey/Aviva/Abi | via abi-ai contracts |
| `role-bindings-max-gemma` | Current | Max/Gemma role bindings | model-alias bindings — not local Qwen/Gemma weights |
| `memory-sqlite-self-learn` | Current | SQLite memory + self-learn | STM/LTM/activity/train_candidate + learn pipeline |
| `memory-map-three-dimensional` | Current | 3-D memory map (topic × recency × consolidation) | deterministic axes — not a learned embedding space |
| `memory-wdbx-in-process` | Current | WDBX in-process memory | behind --features wdbx (off by default); fs4 advisory lock (flock / LockFileEx) |
| `hybrid-loop-route-audit-learn-review` | Current | Hybrid loop / route audit / learn review | Gemma→Max correlated; confidence/alt/fb audit-only; train curation |
| `subagents-local-path-peers` | Current | Multi-subagent + local PATH peers | abbey subagents — same-host peers, not multi-node mesh |
| `improve-goal-driven-loop` | Current | Goal-driven improve loop (local subagents + check.sh bar) | abbey improve — ledger pick + bounded --confirm apply; not multi-node / not unrestricted OS |
| `daemon-read-only-app-core` | Current | shared application core + authenticated read-only abbeyd client/server | abbey daemon status\|claims\|routes uses exact protocol-v1 read-only contracts over an owner-only bounded Unix socket; bounded startup-bound durable runs remain on the separate protocol-v2 surface, and a separate authenticated protocol-v3 envelope serves canonical safe read-only tool inventory, exact canonical stable-claim reads, conditional bounded ABI-local model inventory, and one separately claimed exact-approved safe memory-obsolete effect. Windows named pipes, tool or shell execution on protocol v1/v2, and memory reads/search remain unavailable |
| `daemon-abbey-v1-federation` | Partial | strict abbey.v1 local federation interface | the owner-only Unix daemon and send-once reference client enforce revision-2 corpus and capability-manifest digests, 1 MiB big-endian JSON framing, depth and collection limits, request correlation, typed errors, and no legacy downgrade; Hello, GetStatus, capability inspection, manifest preview, and bounded empty event polling are implemented, while every authority-bearing method is declared but explicitly disabled until its durable authorization, consent, WDBX, cognition, or execution adapter is integrated |
| `app-core-route-audit-read` | Current | sanitized read-only persona/role route-audit contract | abbey daemon routes and the desktop Routes view read a bounded tail (at most 50 entries) of the append-only route.jsonl through one app-core command. The raw working directory never crosses the boundary: it becomes an opaque domain-separated ws-<12 hex> digest, free text is bounded, control-stripped, and path-redacted, and confidence is quantized to whole percent. The producer sanitizes and the consumer re-validates, so an absolute path, a control character, or a non-digest workspace is rejected on the wire rather than trusted. This is a tail read, not a cursor: no sequence column, no filtering by persona/role/workspace, no live subscription, and no write, delete, or rotation path. It is deliberately NOT registered as an MCP tool, because that registry is also served over an unauthenticated loopback HTTP transport |
| `daemon-protocol-v2-bounded-runs` | Current | authenticated protocol-v2 bounded local run control | typed DaemonClient + abbeyd expose only startup-bound ABI-local/Foundation Models submission, durable status/cancel, and snapshot-consistent bounded paged sanitized lifecycle events over the owner-only Unix socket; execution now uses Abbey's finite-budget ABI ModelProvider bridge over those fixed startup objects. The CLI and TUI slash surface emit the same typed AppCommand values and consume AppEvent values through one deterministic frontend-neutral lifecycle reducer. The protocol-v1 read-only command set remains compatible. Requests cannot select executables, argv, environment, or workspace; on protocol v2, tool/shell dispatch, raw prompts/provider output, live subscriptions, daemon-owned memory, automations/approvals, the desktop bridge, model registry lifecycle, and Windows named pipes/Job Objects remain unavailable |
| `daemon-protocol-v3-abi-model-inventory` | Current | authenticated protocol-v3 safe tool, memory, model, and stable-claim authority | abbeyd accepts a separate strict protocol-v3 envelope over the owner-only authenticated Unix socket and always negotiates list_tools and invoke_tools. The first three descriptors project the structurally read-only canonical SAFE_TOOLS registry shared with MCP; the default safe daemon alone appends abbey_memory_mark_obsolete, a local mutating descriptor accepting one bounded opaque record ID, and negotiates decide_tool_approvals plus cancel_tools. The personal catalog and MCP remain exactly three read-only tools and do not receive mutation, decision, cancellation, or execution authority. InvokeTool validates the registered schema before ABI EffectScopedPolicy. Read-only calls retain digest-only authorization and terminal outcome audit, ABI ToolExecutor execution, one-second cooperative deadline, 32 KiB terminal JSON bound, and durable single-use call IDs recovered after reopen. For the local mutation, policy must return RequireConfirmation; the first exact call computes a domain-separated SHA-256 digest over call ID, tool ID, and canonical JSON input, persists one schema-v5 pending record with at most fifteen minutes lifetime and one append-only event, returns a typed correlated pending status, stores no raw input, and performs no effect. ApproveTool and DenyTool bind a globally single-use decision ID to that exact call and digest; CancelTool binds a globally single-use cancellation ID and may terminate only pending or approved records. Denied, cancelled, expired, consumed, mismatched, missing, and reused calls fail closed. Only an identical explicit InvokeTool resubmission of a still-approved call can execute. The daemon atomically changes that approval to consumed and writes schema-v6 prepared intent before opening the startup-selected edition memory backend; requests cannot select or override the backend. It then marks the one record obsolete without deleting provenance, returns a bounded correlated succeeded or failed result, and persists only its SHA-256 digest plus append-only execution transitions. Missing or unavailable memory becomes a bounded terminal failure rather than an unconsumed retry. Debug-only process failpoints immediately after prepare and immediately after the effect prove both crash windows: reopen changes unfinished prepared intent to interrupted, never fabricates a result digest, rejects replay of the consumed call, and requires a fresh call and approval even when the idempotent effect may already have happened. V3DaemonSession sends every request once; approval and resubmission are separate explicit caller actions, and neither downgrade nor automatic mutation replay exists. The daemon also negotiates read_memory only when the startup-selected exact edition backend opens, exposes one explicit memory-v1-summary space, and serves bounded summary-only search plus sanitized metadata. Internal record IDs become domain-separated SHA-256 identifiers; paths are redacted, controls removed, labels capped at 256 bytes, and payload, provenance, source metadata, and raw IDs never cross the socket or participate in search. Search watermarks are cached, bounded, and bound to the exact query so changed stores, future cursors, and cross-query replay cannot drift a page. The same authority always negotiates read_claims_by_id for exact stable-ID reads and conditionally negotiates read_models when an ABI-local fixed provider is bound at startup or a startup-configured local model manifest directory validates; route-derived inventory comes from the same startup-owned ABI ModelProvider route used by protocol-v2 execution, while manifest-derived inventory projects abi-models registry identities with size-checked artifact presence only — available or not_downloaded — never digest verification, license resolution, download, load, or inference. A malformed manifest directory refuses daemon startup instead of serving a partial inventory, and requests cannot select or change the manifest directory or the storage root. Missing or forged grants, unknown spaces and records, future cursors, and non-exact claim IDs fail closed. Protocol v1 and v2 remain unchanged, and MCP receives no memory surface. There is no fuzzy claim search, semantic-vector search, general raw memory authority, served model inference/download/load command, personal shell, training, worker, polling, CLI memory presentation, desktop, Windows, or remote authority |
| `daemon-protocol-v3-model-lifecycle` | Current | signed protocol-v3 local model lifecycle | when an edition-scoped owner-only startup document validates completely, abbeyd verifies a bounded JSON array of Ed25519 publisher-signed ABI model manifests against explicitly configured public keys, opens the exact principal's license-acceptance ledger, requires absolute external storage outside the daemon workspace and Abbey source tree, and selects one exact CPU, Metal, or CUDA device policy without fallback. Only then does negotiation advertise read_models, download_models, and manage_models. DownloadModel and LoadModel bind one immutable model ID, revision, and globally single-use operation ID; license acceptance is checked before admission, one model revision permits only one concurrent lifecycle action, and requests cannot select paths, URLs, trust keys, principals, devices, executables, environment, or a substitute model. ABI's bounded HTTPS range transport plus resumable hash verification and atomic publication back DownloadModel; LocalModelProvider re-verifies every artifact and exact acceptance before loading. UnloadModel drops only the exact in-process provider. Schema v7 keeps at most 4,096 durable operations with monotonic queued, running, succeeded, failed, or cancelled state and progress; queued/running work reopens as failed rather than replaying an ambiguous effect. Typed download and lifecycle status methods correlate every response and send each request once. A loaded provider is not installed as a global default or silently routed into protocol v2; InferenceStatus reports load/unload operation state or explicitly distinguishes loaded-with-no-inference-evidence as available at zero progress. Audit rows contain bounded IDs, revision, operation kind, progress, requested device, and no-fallback evidence, but no registry URL, storage path, principal, license text, weights, tokenizer bytes, or credentials. Protocol v1/v2, MCP, and safe/personal compile-time boundaries remain unchanged. Exact fixture inference is claimed separately by daemon-protocol-v3-exact-model-inference. No production Gemma architecture, real external HTTPS download evidence, model-operation cancellation command, CLI/desktop presentation, Windows transport, remote access, training, or worker authority is claimed |
| `daemon-protocol-v3-exact-model-inference` | Current | bounded exact-loaded-model protocol-v3 inference | when and only when signed model-lifecycle startup authority exists, abbeyd separately negotiates infer_models and synchronously runs one already-loaded exact model ID and immutable revision through ABI LocalModelProvider and run_provider. Requests carry a domain-separated SHA-256 digest over exact identity, a non-empty prompt of at most 32 KiB, and 1..=256 output tokens. Results bind model, revision, request and output digests, at most 32 KiB output, prompt and output token counts, requested and executed device, positive native-operation evidence, and explicit false no-fallback/no-mixed-execution evidence. One-second, event, token, and bounded-capture ceilings apply; tools, route/default installation, substitution, retry, downgrade, and prompt/output persistence are absent. The loaded provider remains process-local behind an Arc, and the existing exact-key active guard serializes inference against lifecycle actions without holding the loaded-map lock during execution. V3DaemonSession sends once and revalidates grant, event kind, model/revision, and both digests. Current evidence is generated abi-bigram-v1 CPU-fixture-only, synchronous, non-durable, and owner-only Unix. No CLI, desktop, MCP, Windows, remote, protocol-v2, default route, production Gemma, production weights, quality, performance, or residency claim is made |
| `daemon-protocol-v3-memory-reads` | Current | sanitized protocol-v3 memory inventory, search, and metadata | the authenticated owner-only Unix daemon negotiates read_memory only when its startup-selected exact edition backend opens. It exposes one explicit memory-v1-summary space: search considers sanitized summaries only, while metadata returns the same bounded projection. Internal IDs become domain-separated SHA-256 identifiers; paths are replaced with [path], controls are removed, and labels stop at 256 bytes. Payload, provenance, source metadata, project paths, and raw record IDs are neither searched nor serialized. A bounded sixteen-entry cache binds each fixed watermark to the exact query and at most 1,024 live records, so later pages remain stable across writes and future, evicted, or cross-query cursors fail closed. Requests cannot select a backend or state root. Default, WDBX-feature, and personal binaries share the contract against their distinct edition roots. The desktop consumes that same typed client through enumerated read-only Tauri commands that request ReadMemory, ReadModels, and ReadClaimsById. MCP, protocol v1/v2, memory mutation, semantic-vector search, windowed GUI packaging, Windows, remote access, and live subscriptions are unchanged or absent |
| `runtime-legacy-conversation-metadata-migration` | Current | idempotent legacy conversation metadata migration | daemon startup snapshots only canonical edition-scoped chat-id/history/by-cwd metadata into a retained owner-only content-addressed backup before idempotently importing opaque conversation identity and trustworthy timestamps into runtime.sqlite; original metadata and finalized backups are never deleted. No transcript, semantic-memory, backend/title/run inference, override-path following, or protocol/UI authority is added |
| `runtime-conversation-identity-write-cutover` | Current | canonical conversation identity mutations and selected compatibility reads | on Unix, new conversation identity saves and scope/all clears validate and domain-separate the active edition and affected scopes before committing monotonic opaque save evidence or explicit tombstones to runtime.sqlite. The same owner-only fs4-locked recovery journal then projects exact edition-scoped compatibility-mirror creation or removal: reopen discards prepared-but-uncommitted plans, completes exactly marked commits idempotently, refuses divergent destinations, and prevents stale identity resurrection. Scope clear removes only the uniquely authorized active working-directory mirror, preserving the global fallback; non-per-cwd scope clear removes global plus export, while clear-all removes global/export plus an exact bounded inventory of direct by-cwd mirrors for the active edition. Reads recover the journal, select cwd before global from canonical edition-scoped scope evidence, continue past cwd tombstones, stop at global tombstones, and accept a selected mirror only when its external identity matches the canonical digest; a corrupt selected cwd mirror never falls back to global. Untracked scopes retain bounded legacy cwd-then-global compatibility. Provider create, resume, retry, and capture paths use the fallible resolver and fail closed on canonical or mirror corruption; the lossy wrapper is presentation-only. Aliases and conversation provenance, history, transcripts, semantic memory, model/route/run data, and retained migration backups are not deleted. Backend/title/run inference, protocol/UI/MCP surfaces, and Windows runtime authority are unchanged or unavailable |
| `inventory-media-delegation-surfaces` | Current | MCP/ACP inventory + voice + highlight + media/imagine | inventory/peer launch and delegated media surfaces; Abbey is not a client/host of other providers' MCP servers, and there are no local neural media models yet |
| `mcp-server-stdio-readonly` | Current | Abbey read-only stdio MCP server (tools capability) | abbey mcp serve — JSON-RPC 2.0 over newline-delimited stdio, protocol-version negotiation, tools/list + tools/call with JSON Schema 2020-12 descriptors, over a registry whose effect type admits read-only tools only (no shell, no filesystem mutation, no process spawn — abbey_platform therefore omits accelerator detection, which spawns vendor binaries); enforced request-byte / JSON-depth / batch / timeout / response limits plus outbound secret redaction. The same registry and the same dispatch are also served over loopback HTTP — see mcp-server-http-loopback-readonly. TLS, OAuth 2.1/PKCE, resource indicators, token-audience binding, and the stateless-lifecycle revision are NOT implemented on either transport; serving tools does not make Abbey a client/host of other providers' MCP servers |
| `mcp-server-http-loopback-readonly` | Current | Abbey read-only Streamable HTTP MCP transport (loopback only) | abbey mcp serve http — HTTP/1.1 POST /mcp, one request per connection, routed through the *same* Server::handle_frame and Server::encode_frame as stdio, so the tool registry, the byte/depth/timeout ceilings, and outbound secret redaction are shared code rather than a parallel implementation; a non-loopback --host is a hard startup error, not a warning; two-layer DNS-rebinding defense: Host must be loopback (parsed structurally — authority split plus IpAddr::parse, never substring matching) and Origin, when present, must be a loopback origin (parsed with the url crate), so lookalikes such as http://127.0.0.1.evil.com and http://localhost.evil.com are rejected by both layers; enforced named concurrency cap, fixed-window rate limit, read/write deadlines, request-head and header-count ceilings, and a bounded idle-expiring session table. The transport is UNAUTHENTICATED and Mcp-Session-Id is a routing key, not a credential: loopback bounds who can reach it (no off-host caller), not who may call it (any local process may, while it runs). HTTPS/TLS, non-loopback binding, OAuth 2.1/PKCE, resource indicators, token-audience binding, SSE/GET streaming with resumability, and the stateless-lifecycle revision are NOT implemented and are deliberately not faked |
| `backend-foundation-models` | Current | On-device backend (ABBEY_BACKEND=fm) | macOS 26+ Foundation Models CLI; account/gen refuse; local MCP inventory remains available |
| `backend-abi` | Current | abi backend (ABBEY_BACKEND=abi) — no cursor-agent required | one-shot `abi complete`: deterministic persona-template locally by default; bare claude-* / live\|anthropic opt into abi's Anthropic transport (abi credentials); cursor role/thinking bindings stay local (no silent --live); needs a real `abi` binary; account/gen refuse; local MCP inventory remains available; Abbey-side transcript continuity (bounded context prefix — abi itself stays stateless); default backend selectable via config `backend` key |
| `backend-claude-code` | Current | Claude Code CLI backend (ABBEY_BACKEND=claude) | deterministic backend wiring only: explicit selection or last-in-order automatic fallback resolves a real claude binary without cursor-path fallthrough; Claude receives its own bounded argv grammar, Abbey model aliases clamp to Claude's vocabulary, successful first use leaves only a local session-presence marker, and later turns use Claude's own resume surface. Cursor/fm/abi flags and cursor account verbs are not forwarded. No live Claude account, plan entitlement, catalog availability, response quality, or vendor-runtime parity is claimed |
| `portable-primary-host-surfaces` | Current | linux/macos/windows/unix primary host targets (portable surfaces) | CLI/TUI/sqlite/WDBX-lock/subagents/os-allowlist; PATHEXT which_bin; voice adapters on all primary hosts (macOS say/Speech Current; whisper-cli/whisper-cpp + espeak when their required binaries and model artifacts exist; Piper requires ABBEY_PIPER_MODEL plus a host audio player; SAPI requires PowerShell); fm macOS-only; no bundled speech weights; live Discord and GUI learning are not Current |
| `subagents-threaded-fanout` | Current | multi-threaded subagent fan-out (--jobs) | std::thread parallelism sized from available_parallelism — not GPU kernels |
| `memory-wdbx-cross-process-lock` | Current | WDBX cross-process lock (Unix + Windows) | fs4 advisory lock (flock / LockFileEx); behind --features wdbx |
| `accelerator-presence-detection` | Current | GPU/NPU/TPU presence detection (report-only) | abbey platform compute — inventory only, not Abbey accelerators |
| `accelerator-kernel-execution-metal` | Current | verified Metal numerical kernel execution (dot / cosine / top_k) | abbey accel verify behind --features accel (off by default) — three numerical kernels (dot, cosine, and top_k over 1024-element f32 vectors) execute through abi-gpu's MetalAccelerator, and every natively produced value is checked against the deterministic abi-compute CpuBackend oracle in the same process and the same run: floats at relative tolerance 1e-3 against max(\|native\|, \|oracle\|, 1.0), top_k indices compared exactly. Native execution is read from the adapter's CapabilityState evidence ladder with a fresh adapter per kernel, never inferred from the return value, because the adapter returns the oracle itself on fallback and a comparison would then be vacuous; a run that never reached Metal reports `backend used: cpu`, records no parity, and exits 1, while a partially native run reports `mixed`. Live-verified from an isolated installed directory on an Apple Silicon host. install.sh treats the CLI, Unix daemon, and ABI Metal dylib as one rollback-protected publication unit; it verifies that both executables depend on @loader_path/libabi_metal_dot.dylib, verifies that identity on the copied dylib with otool, executes the exact staged and published layouts, and restores every prior destination byte on staged-verification or mid-relocation failure. Unsupported or unverifiable platform layouts refuse before publication. This is kernel execution, numeric parity, and loader-relative installation ONLY: it is NOT GPU/NPU/TPU compilation, training, or model inference, which remain Proposed under accelerator-execution-runtime; NOT ANE/GPU device residency or placement, which neither Metal nor CoreML exposes here; NOT CUDA, Vulkan, or TPU, whose adapters stay build-detection only with no runtime evidence; and NOT any speedup or performance result, because nothing in this path is timed. |
| `cot-transcript-viewer` | Current | CoT transcript viewer (`abbey cot`) | saves/displays structured reason output — not an Abbey CoT engine |
| `runtime-responsibility-matrix` | Current | tool responsibility matrix (`abbey runtime`) | documents who executes what; Abbey is not the tool host |
| `honesty-oos-surfaces` | Current | OOS honesty surfaces (`abbey oos` / lora\|weights\|accel\|shell\|host) | status + refuse for deferred capabilities — does not implement them |
| `memory-lexical-similarity` | Current | lexical similarity search over memory (feature-hash cosine) | abi-ai n-gram hash + cosine at query time — independent of learned providers |
| `memory-semantic-embedding-space` | Current | semantic / learned memory embedding space | opt-in apple\|openai provider; space-isolated SQLite/WDBX persistence; Apple paraphrase ranking locally verified; remote live call unverified |
| `memory-metadata-filters` | Current | memory filter by source / timestamp / project | shared exact metadata plus inclusive RFC 3339 bounds on SQLite and WDBX |
| `mesh-abi-authenticated-local-multiprocess` | Current | ABI WDBX authenticated local multi-process proof | abbey mesh local-demo on Unix; 3..=9 loopback ABI processes; not production multi-VM |
| `program-1-abbey-contracts-host` | Current | Program 1 Abbey contract corpus host | C1 data-only native validation of the exact ABI corpus at aggregate digest 72e241e34967df318376bf68f4a0e2db13f5ebf17d1a219709731f1f470dbe8e; qualification verifies closed lock metadata, every committed byte, local-only JSON Schema compilation, and synthetic fixture dispositions. This adds no production federation authority and records no participant-consented live Discord evidence |
| `program-3-guild-intelligence-synthetic` | Current | Program 3 closed synthetic guild intelligence | C2 closed synthetic replay over explicitly synthetic, already-loaded metadata recordings. The pure app-core engine validates exact reference, object, overwrite, relationship, permission-order, and unknown-target boundaries; normalizes five twin views and per-object digests; derives metadata-only findings; and emits distinct operator-selectable plans whose digests bind observed preconditions, desired states, expected postconditions, and rollback previews. Independent owner and administrator recordings replay deterministically to distinct canonical artifacts. The fixed redacted status remains stale, read-only, and based only on a synthetic fixture claim. There is no non-synthetic capture, live Discord access, participant consent, persistence, approval, execution, deployment, transport, filesystem, process, durable store, runtime tool, content, or effect surface |
| `edition-compile-time-separation` | Current | compile-time edition separation (identity, config root, credential namespace, audit path) | one Edition source selects product/binary/bundle name, config + state roots, credential env namespace, daemon socket, and audit log path; the default build stays the safe edition with unchanged paths — separation only, no unrestricted runtime in either edition |
| `edition-windows-install-naming` | Partial | edition-scoped Windows install names (install.ps1) | install.ps1 derives the installed binary, daemon, and completion names from the compiled edition (`abbey edition --name`/`--daemon-name`) instead of literals, so a personal-edition build can no longer overwrite the safe install's abbey.exe or _abbey.ps1; the release artifact path keeps the edition-independent cargo bin name. Proof is source parsing only — no PowerShell runtime exists on the development machine, so no Windows install has been executed, and no daemon, packaging, signing, or notarization is delivered for Windows |
| `desktop-tauri-react` | Proposed | Tauri 2 + React/TypeScript desktop GUI | approved product direction; safe-edition macOS Routes-only window acceptance against a real scratch abbeyd passed on 2026-09-06, but packaged desktop-product acceptance remains unproven and the shipped interactive UI remains the ratatui TUI |
| `runtime-provider-neutral-owned` | Proposed | provider-neutral Abbey-owned agent and tool runtime / MCP-ACP client-host | approved product direction; Abbey now routes fixed startup-owned providers through ABI ModelProvider contracts and serves bounded ABI-local inventory plus one exact-approved safe memory-obsolete effect over authenticated v3 grants. It still has no provider-neutral multi-tool loop, general execution registry, or external MCP/ACP client-host. Abbey serving its own read-only tools over stdio is the opposite direction and does not close this broader roadmap claim |
| `models-production-local-weights` | Proposed | production-capable local model weights | approved product direction; Max/Gemma names currently remain role bindings, not bundled weights |
| `training-lora-pipeline` | Proposed | fine-tuning / LoRA pipeline | approved product direction; train_candidate remains curation substrate and performs no weight updates |
| `accelerator-execution-runtime` | Proposed | GPU/NPU/TPU compilation, training, and inference in Abbey | approved product direction; current compute commands detect and report hardware only |
| `media-local-neural-models` | Proposed | local neural speech / image / video models | approved product direction; current voice is platform I/O and current media generation is delegated to agent tools |
| `edition-personal-unrestricted` | Proposed | personal-unrestricted runtime (separate edition) | the compile-time separation mechanism ships (see edition-compile-time-separation); the unrestricted runtime does not — no shell.exec, no signed privileged helper, no always-on unrestricted mode, and no separate packaged bundle in any edition; shipped Abbey keeps allowlist + --confirm |
| `mesh-three-vm-and-production` | Proposed | authenticated local three-VM shared-compute proof, then production separate-host / geographic-HA / multi-GPU mesh | approved product direction; the authenticated local multi-process proof does not establish separate-VM deployment |
| `ci-self-hosted-linux-proof` | Blocked | self-hosted Linux CI execution proof | Abbey pins public ABI 243082d06f64ff7a68b82ea296f480fcf7f6df68 and public WDBX 62ac490f962df0f73fc875c1e294393474d329ad without a checkout secret; exact-head macOS ARM64 self-hosted runs succeeded and forks have a hosted-safe gate that has not yet executed for a foreign fork; Linux ARM64 is not provisioned and Windows runtime proof remains open |
| `oos-shipped-shell-bypass` | Out of scope | unrestricted shell or allowlist bypass in the shipped edition | the shipped edition keeps os_control allowlist + --confirm as a safety invariant; only the separately packaged personal edition is Proposed |
| `oos-owned-cot-engine` | Out of scope | Abbey-owned chain-of-thought engine / interactive CoT UI | reason uses Cursor thinking models; cot is a transcript viewer only |
| `oos-fake-cost-accounting` | Out of scope | fake cost / token accounting | /cost stays N/A for cursor-agent |
| `oos-bundled-cloud-speech` | Out of scope | bundled cloud TTS/STT SaaS | cloud speech services are not bundled; local neural speech is Proposed separately |
| `oos-reimplement-vendor-runtimes` | Out of scope | reimplement Grok/Codex/Claude runtimes | surface parity only |
<!-- END abbey-generated:claims-table -->

Canonical agent guidance for this repository. `CLAUDE.md` is a pointer that
keeps only the command table and the `src/` module map (verified against this
file and the tree by `tools/check_instructions.py`); the execution-path diagram
and the docs map live in [docs/architecture.md](docs/architecture.md). Only
marked claims regions are generated by `tools/check_claims_sync.py` from
`src/claims.rs` and workflow ledgers. Edit prose outside those regions;
capability changes belong in the registry, not the generated table.

## Project

Abbey — a hybrid persona/role CLI/TUI written in Rust, backed by pluggable executor backends: `ollama` (preferred default, local `gemma4:26b-mlx`) · `grok` · on-device `fm` · the sibling `abi` CLI · the `claude` CLI · optional `cursor-agent`. It mimics compatible surfaces from Grok Build, Codex, and Claude Code while routing generation through the selected executor and, for personas/identity, the sibling `abi-ai` crate. No single backend is a hard requirement: local verbs run with none installed, and generation uses whichever executor resolves. Claude wiring is source/unit-tested; live account, model availability, and vendor-runtime parity are not claimed.

Brand: **Intelligence Without Limits** — with a claims ledger. IWL is Abbey/ABI only; Quesar keeps Private AI operations and does not inherit this tagline. See [docs/brand.md](docs/brand.md).

## Need to know (the three things that bite)

1. **Toolchain**: pinned `nightly-2026-09-01` (Rust 1.100.0). Homebrew `rust` shadows `rustup` — run `rustup run nightly-2026-09-01 cargo ...` or `brew unlink rust`.
2. **Sibling repos required**: `../abi` and `../wdbx` are path dependencies. Even default builds fail without them. CI pins revisions in `.github/workflows/rust.yml`.
3. **Gate is `./check.sh`, not `cargo test`**. It runs all four feature modes (default, `wdbx`, `personal-edition`, `accel`) plus Python claims/program-3 checks. A bare `cargo test` compiles none of the three feature-gated modes.

## Gates (exact commands)

- `./check.sh` — production gate (toolchain probe → fmt → clippy/tests × 4 modes → rustdoc × 4 → Python tools → claims sync → instruction drift → desktop IPC codegen drift → Program 3 boundary → installer smoke → file-size guard → soft cross-compile → opt-in coverage)
- `cargo build --features wdbx` — in-process WDBX backend (off by default)
- `cargo build --features personal-edition` — separately named personal edition (distinct binary/config/credential namespace)
- `cargo build --features accel` — Metal kernel verification (needs macOS Xcode toolchain)
- `cargo test --features wdbx|personal-edition|accel` — run feature-gated tests
- `python3 tools/check_claims_sync.py [--write]` — verify/generate claims ledger
- `python3 tools/check_p3_readonly.py` — enforce Program 3 read-only boundary
- `python3 tools/check_instructions.py` — CLAUDE.md stays a pointer (command table + module map only), git-policy block matches, mapped modules and scripts exist
- `desktop/check.sh` — separate workspace; the root gate runs only its codegen drift check (`--locked`, no Tauri build)
- `ABBEY_CARGO_FEATURES=personal-edition ./install.sh` — install personal edition

Full command list: the table in `CLAUDE.md`.

`./check.sh` is the code-change merge bar. For docs-only edits use the scoped
checks listed above; the claims synchronizer itself invokes `cargo run`. In order:

1. Toolchain probe: a cheap `cargo check` that trips the `rust-version` gate during unit-graph construction, so a shadowed Homebrew cargo fails fast with the remedy printed.
2. `cargo fmt --all -- --check`.
3. Clippy `-D warnings` + tests, interleaved per build mode for **all four build modes** (default · `wdbx` · `personal-edition` · `accel`).
4. Warning-denied private-item rustdoc for each of the four modes.
5. Python tool tests (`tools/tests/`), then claims synchronization (`tools/check_claims_sync.py`) and the instruction-file drift check (`tools/check_instructions.py`), then the desktop IPC codegen drift check (`desktop/codegen -- --check`, `--locked`; builds only the codegen crate).
6. Program 3 read-only boundary check (`tools/check_p3_readonly.py` fails closed if `src/app_core/guild_intelligence.rs` acquires network/process/fs/store/tool code).
7. Installer checks (`sh -n` plus `tools/tests/smoke_accel_install.sh`).
8. File-size guard.
9. *Soft* cross-compile checks for `x86_64-pc-windows-gnu` / `x86_64-unknown-linux-gnu` (skipped when the required cross toolchain is unavailable, and never a hard pass).
10. Opt-in soft coverage report (`ABBEY_COVERAGE=1`, needs `cargo-llvm-cov`; report-only, never a gate).

A bare `cargo test` never compiles `src/memory/wdbx.rs` or the edition- and accelerator-gated code, so it can pass while a gated surface is broken.

`tests/` includes process-level CLI suites plus `app_core_contract.rs`, which imports Abbey as an external library client. Process tests drive `CARGO_BIN_EXE_abbey` (the daemon suites also need `CARGO_BIN_EXE_abbeyd`, the second binary from `src/bin/abbeyd.rs`) because some guarantees only exist once the process runs — real exit codes, real stdout/stderr, and the SIGPIPE reset before `main`. `daemon_cli.rs` starts real `abbeyd` and `abbey` binaries against owner-only scratch state; it must never use a user's socket or bearer. `cli_surface.rs` uses a throwaway `ABBEY_STATE_DIR`; keep that property for state-mutating cases. `slash_parse.rs` is read-only/current-dir scoped. The app-core contract test must stay presentation-neutral and must not gain crate-private access.

## Toolchain

Rust **nightly-2026-09-01** (`rustc 1.100.0-nightly`), edition **2024**, pinned via `rust-toolchain.toml` (`rustfmt` + `clippy` components). Rust has no edition 2026; edition 2024 is the current language edition, while the dated toolchain makes the 2026 compiler baseline reproducible. `Cargo.toml` sets `unsafe_code = "deny"` as a manifest lint (clippy `-D warnings` alone would not catch it), so every `unsafe` needs a justified `#[allow(unsafe_code)]` with a SAFETY comment.

**Both sibling checkouts are required even for default builds.** `Cargo.toml`
unconditionally consumes `abi-ai`, `abi-agent-host`, `abi-agent-runtime`,
`abi-model-runtime`, `abi-models`, and `abi-worker` from `../abi/crates/`; those
already depend on `../wdbx`. `--features wdbx` enables Abbey's in-process memory
backend; `--features accel` adds `abi-gpu`, `abi-compute`, and
`abi-model-runtime/metal`. Keep all three repositories adjacent and avoid mixing
git/path sources of shared crates. CI pins `ABI_REVISION`, `WDBX_REVISION`, and
the pinned ABI revision's own `ABI_TOOLCHAIN` in `.github/workflows/rust.yml`;
these need not match the current local siblings. Both `abbey wdbx` and
`ABBEY_BACKEND=abi` need a real executable: run `./tools/cargo.sh build -p abi-cli`
inside `../abi`, then point `ABBEY_ABI_BIN` or config `abi_bin` at it. A shell
alias does not satisfy subprocess resolution. `build.rs` watches Git HEAD and
the branch ref for the build stamp.

## Execution path

Abbey has one canonical execution path shared by the CLI, slash commands, and the TUI — do not add a second way to invoke the agent.

Three headless capture surfaces are the standing exceptions, all routed through
the shared `capture.rs` (`run_print` / `capture_chat`) rather than calling the
executor ad hoc: `print` (`commands.rs`), `commit` (`actions::run_commit`), and
`voice ask` (`voice.rs`). None reach `run_agent`, so they skip `hybrid_run`
entirely — no persona/role wrap, no prefs injection, and **no `route.jsonl`
entry** (enforced by `print_bypasses_the_route_log_where_ask_appends` in
`tests/cli_surface.rs`: `abbey print …` leaves the route log unchanged where
`abbey ask …` appends a row). That is deliberate for single-shot piping and
spoken replies, but it means the routing audit does not see them. New bypasses
must go through `capture.rs` and be added to this list — its caller set *is*
the bypass inventory.

The layer diagram is in [docs/architecture.md](docs/architecture.md#execution-path).

Personas (Abbey/Aviva/Abi) and Max/Gemma worker roles are defined in the sibling `abi-ai` crate (`../abi/crates/abi-ai/src/identity.rs`) — Abbey's own code consumes those contracts rather than redefining identity. See [docs/identity.md](docs/identity.md) for the distilled spec and Current/Proposed status of each claim.

## Conventions

- **File size is enforced by `check.sh`, not just style**: `main.rs` must stay under 200 lines (hard fail); other `.rs` files warn past 800 and hard-fail past 1000 lines. Split modules before hitting the ceiling rather than after.
- Prefer small, reviewable diffs that match existing style.
- Only commit when asked; never force-push `main`; never commit secrets.
- Conversation mirrors are projections: commit the opaque canonical save or clear
  tombstone first, then recover only the exactly marked creation/removal plan through the
  owner-only locked journal. Identity clear must never delete history, transcripts,
  memory, models, route/run data, or finalized backups. Provider-facing reads must use
  the fallible canonical selector; only presentation may use the lossy mirror wrapper.
- **Keep claims honest** — this project explicitly tracks what is shipped ("Current") vs. designed-only ("Proposed") vs. explicitly deferred ("Out of scope") in this file's claims-gate table and in the docs. `ollama` is preferred, while alternate CLI wiring does not reimplement Grok/Codex/Claude runtimes; Max/Gemma default to the local `gemma4:26b-mlx` Ollama tag, not bundled Abbey weights; `/cost` is intentionally N/A. Don't let new code or docs imply otherwise. A capability behind an off-by-default feature is "Current behind `--features X`", not plain Current — and only if the gate compiles and tests it.
- **A feature-gated module is invisible to the default gate.** If you add another `[features]` entry, add matching `clippy`/`test` lines to `check.sh`, or the code can rot while CI stays green.
- **Each backend gets its own argv grammar** (`src/agent/argv.rs`). `fm`, `abi`, `claude`, and `ollama` share none of cursor-agent's flags; their builders start from scratch, and tests assert no foreign flag leaks. Under `fm`/`abi`, don't let `hybrid_run` inject role→model ids (`fm` vocabulary is `system|pcc`; under `abi` a cursor `claude-*` binding would look like an explicit live request). Under `ollama`, role aliases collapse to `gemma4:26b-mlx`. Claude maps Abbey aliases into its own vocabulary and uses a marker only to select `--session-id` versus `--resume`; Claude owns the transcript.
- **ollama is preferred, never required.** Backend precedence (`src/agent/backend.rs`) is `ABBEY_BACKEND` env > config `backend` key > the legacy `ABBEY_AGENT` cursor path > ollama when resolvable > grok → fm → abi → claude, then cursor last; a set-but-unknown `ABBEY_BACKEND` value selects ollama and does not fall through to config; `doctor` shows an automatic choice as `backend: … (from auto …)`. Executor resolution is best-effort at startup and mandatory only at spawn time (`AgentConfig::exec_path`), so every local verb works on a machine with no executor installed. `ABBEY_BACKEND=ollama|abi|claude` runs without cursor-agent. Continuity under `abi`/`ollama` is Abbey's bounded context transcript; Claude continuity remains in Claude's own session store.
- **Never branch per-call behaviour on `AgentBackend::from_env()`** — it is resolved once per process, while the TUI's Ctrl-B switch changes `AgentConfig::backend` at runtime. Thread the live `cfg.backend` through instead (`state.read_chat_for(cfg.backend)`, `AgentBackend::transcript_subdir()`). Reading the cached value is what let a cursor-launched session keep adopting `CURSOR_AGENT_CHAT_ID` after switching to `abi`, silently killing continuity — a guard that consults the wrong backend is not a guard.
- OS execution (`os_control.rs`) must never run without `--confirm`, and only against the allowlist, in both editions, with no shell bypass — this is a safety invariant, not a default to relax.
- **`WdbxMemory` must hold its `fs4` advisory lock for the handle's whole life** (Unix `flock` / Windows `LockFileEx`). It acquires `<dir>/abbey.lock` before recovery and drops the store before releasing it. `wdbx_bridge` takes that same lock for own-store subprocesses. The current sibling `DurableStore` separately holds `<base>.writer.lock`; the old claim that it has no cross-process lock is stale. Preserve both coordination layers.
- The lock does not extend to an `abi` you invoke directly against the same store, and `./install.sh` builds without `wdbx`, so an installed `abbey` asked for wdbx falls back to SQLite and says so in `doctor`. Installers name binaries from the compiled edition probe (`abbey edition --name`) so the two editions cannot clobber each other; `install.ps1`'s naming is proven by a parser test only, no Windows host has run it, so that claim is Partial.
- Read-only callers should use `memory::backend_path` (pure) rather than opening, and interactive ones `open_backend_with_timeout` — `learn status` once created the very store it was meant to report on, and the TUI redraw would otherwise stall 10s on a lock (the TUI memory panel uses a 250 ms open timeout and renders `unavailable: …`).
- **`abbey daemon` and `abbeyd` must use the same owner-only Unix socket and exactly one bearer source** (`ABBEYD_SOCKET_PATH`; `ABBEYD_BEARER_TOKEN` xor `ABBEYD_BEARER_TOKEN_FILE`, owner-only). Client failures never fall back to in-process claims.
- **`desktop/` is a second cargo workspace** (Tauri 2 + React/TypeScript client of the app core) with its own `Cargo.toml`, `check.sh`, and `package.json`, and its TypeScript IPC types are generated from `src/app_core/`. `./check.sh` does not build it, but runs `desktop/codegen --check` with `--locked`, so changing `app_core` without regenerating those types fails the root gate; when `desktop/Cargo.lock` is stale against the siblings that step cannot resolve and prints `WARN … UNMEASURED` instead of passing.
- `abi wdbx` takes **base paths** (parent dir + base name) while Abbey opens a **directory** — Abbey's `<state>/wdbx/` is `<state>/wdbx/wdbx` to `abi`. `wdbx_bridge` translates; passing the bare directory silently reads one level up and reports an empty store.
- Self-learn's `train_candidate` path requires provenance; don't add silent deletes to the reflect/digest flow.
- State (`~/.local/state/abbey`, including `memory.sqlite`) is runtime data — never commit it, and don't assume it exists in a fresh checkout.
- Learned embeddings: opt-in only; keys from `ABBEY_EMBEDDING_API_KEY` / `OPENAI_API_KEY` (not config.toml); provider/model change = new isolated vector space.

<!-- machine-git-policy -->
## Git workflow (machine policy, 2026-08-27)

Work on the default branch in this canonical checkout. Do not create
branches or worktrees by default; they are for tasks that genuinely need
isolation, or when Donald asks. Any worktree or topic branch created here
must be merged back into this checkout's default branch, the worktree
removed, and the branch deleted, before pushing and before the task is
called done. Full policy: `~/.claude/CLAUDE.md` (*Git discipline*).
<!-- /machine-git-policy -->
